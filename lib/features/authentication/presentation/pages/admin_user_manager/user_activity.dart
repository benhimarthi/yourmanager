import 'package:flutter/material.dart';

class UserStatu extends StatelessWidget {
  const UserStatu({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
